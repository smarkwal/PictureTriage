package net.markwalder.picturetriage.domain;

/**
 * Represents a final decision for an image in Phase 3 (final review).
 * Each image in the grid can be marked for either KEEP or DELETE.
 */
public enum Phase3Decision {
    KEEP,
    DELETE
}
